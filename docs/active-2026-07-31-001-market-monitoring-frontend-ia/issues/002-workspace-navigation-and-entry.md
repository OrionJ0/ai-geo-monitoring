---
title: "新侧边栏与固定默认入口"
status: closed
type: AFK
blocked_by:
  - "001-default-market-project.md"
---

# 新侧边栏与动态默认入口

## Parent

- [PRD](../prd.md)
- [Tech Spec](../TECH-SPEC.md)
- 覆盖用户故事：US-003、US-005、US-006、US-008

## What to build

将工作台侧边栏重组为市场总览、投放与流量、转化结果、AI品牌监测、网站诊断、监测任务、常用网站和设置等语义明确的入口，并让菜单名称、选中态和默认入口来自同一导航合同。

完整信息架构立即向内部用户展示，市场总览成为工作台默认入口。来源能力只控制真实数据读取和刷新，不再隐藏菜单；未接入来源显示中性占位页。工作台设置分组不再提供管理设置跳转。

## Acceptance criteria

- [x] 侧边栏按照PRD确定的分组、名称和顺序展示。
- [x] 原“项目看板”“来源分析”“SEO检测”分别显示为“AI搜索表现”“引用来源分析”“SEO健康检测”。
- [x] 所有来源状态下均展示市场总览、投放与流量、转化结果及现有 GEO/SEO 入口。
- [x] 原始咨询和订单结果在无来源 API 时进入各自的只读占位页。
- [x] 工作台默认入口固定进入市场总览。
- [x] 菜单和当前选中态对所有直接子路由保持一致，主内容不重复当前页名称。
- [x] 原“系统”分组改名为“设置”，仅保留系统通知和个人中心，不提供管理设置跳转。
- [x] 六个一级分组及全部子项静态展开，不显示下拉触发器。
- [x] 窄屏使用覆盖式侧栏，点击菜单后自动收起；键盘操作、焦点状态和可访问名称通过验证。
- [x] 管理员后台增加返回数据工作台、后台总览和历史记录直接入口；账号与权限、系统管理复用同一静态分组与移动端侧栏交互。
- [x] Header 与 Sider 固定，HTML/body 不滚动，仅主内容区滚动；一级菜单基础字体样式统一。
- [x] 一级菜单基础颜色从近黑调整为中性灰；可点击直接入口保持中性灰，不能点击的分组标题使用更浅灰色，字号和字重一致。
- [x] 常用网站作为设置上方的同级直接入口，菜单和选中态共用导航合同。

## Blocked by

- [001-default-market-project.md](001-default-market-project.md)

## Verification

- `node --test src/utils/geoNavigation.test.cjs src/utils/quickLinksPage.test.cjs`，10 项全部通过。
- 真实浏览器在 1440px 验证菜单顺序完整；375px 默认折叠并以覆盖式侧栏展开，不再挤压主内容。
- 真实浏览器验证六个分组初始即完整展开，DOM 中不存在 `SubMenu`；移动端展开样式与原侧边栏一致。
- 管理员导航合同测试覆盖静态分组和路由选中态。
- 1440×900 验证 HTML/body 均不可滚动、主内容区可滚动 900px，侧栏滚动前后顶部均为 64px。
- 375×812 验证侧栏范围为顶部 64px 至视口底部 812px，移动端根页面无第二滚动条。
- `/geo` 固定进入市场总览，菜单与选中态共用同一导航合同。
- 390×844 移动端覆盖侧栏可滚动至系统通知和个人中心，页面中不存在 `/admin/settings` 链接。
