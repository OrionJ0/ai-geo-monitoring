---
title: "冻结广告资源合同与生产基线"
status: closed
type: HITL
blocked_by:
  - "003 完成 A2 并关闭"
---

# 冻结广告资源合同与生产基线

## Parent

- [PRD](../prd.md)
- [Tech Spec](../TECH-SPEC.md)

## User stories covered

- US-2：关键词和搜索词在数据增长后仍保持有界负载。
- US-4：所有页面通过同一 revision 保持快照一致。
- US-5：迁移前有明确合同、基线和退役条件。

## What to build

在不改变当前 API 的前提下，从生产正式入口测量 Dashboard 响应体积、读取耗时、页面请求和真实消费者，冻结三个新资源的分页、排序、筛选、错误、缓存和 revision 合同，并固定广告层级与关键词的全筛选范围 summary。该证据是后续 R1 实现的门禁，不以 fixture 行数代替生产事实。

## Acceptance criteria

- [x] 003 已完成 A2、生产正式入口验收并关闭。
- [x] 记录 Dashboard 压缩前后字节、数据库读取耗时和可获得的 P95 基线，不记录敏感业务明细。
- [x] 盘点仓库内页面、CLI、诊断脚本及可观测生产调用，确认当前详细数组消费者清单。
- [x] 用真实数据确认关键词和搜索词默认/最大 page size，并冻结为服务端合同。
- [x] 用现役 UI 确认允许的排序、文本查询和父级筛选字段，不加入假想字段。
- [x] 冻结广告层级和关键词 summary 的现役指标字段、精确类型、null 语义及与分页无关性。
- [x] 冻结轻量 Dashboard、广告层级、关键词、搜索词的响应 schema、错误码和缓存规则。
- [x] 证据证明本切片没有修改生产响应、页面默认路径、数据库或刷新行为。

## 2026-08-05 验收证据

- 红灯：新增合同测试首次因 `MarketingAdReadContract` 不存在而以 `MODULE_NOT_FOUND` 失败；最小实现后专项 2/2 通过。
- 生产基线：完整 Dashboard 未压缩 1,066,452 B、gzip 71,178 B；原始读取 30 次 P95 153.53 ms，正式 HTTPS gzip 20 次 P95 297.63 ms；45/192/898/350 四级数组只记录数量，不保存业务明细。
- 正式 Chrome：市场总览、广告表现、关键词、搜索词分别读取 1/1/1/2 次完整 Dashboard，解码字节为 1,066,452 / 317,908 / 317,908 / 648,003。
- 消费者：四个正式页面和对应 hook/adapter 已逐项登记；后端 CLI、诊断脚本与当前 Nginx 留存窗口没有发现额外明细消费者。Dashboard 的 110 次请求由 89 次浏览器访问和本 issue 的 21 次受控 HTTPS 测量构成，新资源路径为 0。
- 合同：默认/最大 page size 为 50/200；允许字段、三个精确 summary 指标、schema、revision、错误和私有缓存均由机器常量与文档双重固定。
- 不变性：本 issue 只新增未接线路由的合同常量、合同测试和证据文档；生产仍运行 `58469e29214ccc28e989f07d54af873d9c0ba801`，现役 Dashboard、页面、数据库、四报表刷新、预算、双读和原子快照均未改变。

## 2026-08-06 过程偏差与补救

- 本 issue 当时只冻结了 `MarketingAdReadContract.js` 常量，没有按后来补充的治理要求在拆分详情资源前先交付正式 OpenAPI 3.1 文件；OpenAPI 实际到 R2 提交 `91d55a93c03931c0dc23b884d7eb476586701a5b` 才加入。该顺序偏差不能被最终形态掩盖。
- 最终四个现役路由、前端生成 wire type 和错误信封已经收敛到唯一 OpenAPI 文件；2026-08-06 的 008 Issue 002 又增加 JSON Schema 2020-12 运行时合同测试，直接校验四个真实服务成功响应与代表性 401/409/422/503 路由响应。
- 新校验首次发现三个详情资源的 `lastSuccessfulAt` 仍是 SQLite 时间文本而非 OpenAPI 声明的 RFC 3339 `date-time`；共享快照选择器已统一输出 ISO 时间。聚焦 17/17、营销全量 244/244 通过。

## Blocked by

- 003 完成 A2、正式入口验收并关闭。
