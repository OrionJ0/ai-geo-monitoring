# 市场数据监控、GEO 与技术 SEO

本上下文统一市场数据监控、GEO 与网站检测报告中的业务术语，确保来源事实、监控指标、问题名称和修复建议表达同一个概念。

## 项目定位判定

**现行产品定位**：
本项目是面向市场负责人的只读市场数据监控系统。GEO/SEO 是当前已经正式运行的能力，百度营销与百度统计处于白名单真实数据试点；按来源串联客服咨询、线索入池、成交订单数和成交订单金额是尚未完成接入与验收的全链路目标。产品定位不等于当前已接入范围；概括项目时必须先说明只读市场数据监控定位，再分别说明正式能力、受限试点和长期目标。
_避免使用_：AI GEO Monitoring System、单一 GEO/SEO 监测系统、把当前导航范围当成产品最终边界

**当前正式工作流**：
已从正式用户入口开放并作为默认业务路径运行的能力。当前指 GEO、AI 搜索可见度和技术 SEO 监测，不包含仍由白名单直接访问的百度营销试点。
_避免使用_：把代码已存在、路由可直接访问或白名单试点称为正式开放

**受限真实数据试点**：
已使用真实来源数据验证，但只对指定项目或管理员开放，且尚未通过全面开放门禁的能力。页面入口可以展示试点或待开放状态，但不能因此扩大真实数据权限。当前指百度营销与百度统计试点。
_避免使用_：正式上线、全面开放、完整营销漏斗

**最终全链路目标**：
在同一监控项目中联合观察 GEO/SEO、付费广告、网站访问、原始咨询和订单签订金额，同时保持来源系统为业务操作入口。
_避免使用_：当前已经打通、全链路业务操作系统

## GEO 网页监测语言

**DeepSeek API 样本**：
平台代码为 `deepseek`，来自正式 API Adapter。它不代表用户在 DeepSeek 官方网页中看到的最终页面结果。
_避免使用_：DeepSeek Web、网页版样本

**DeepSeek Web 样本**：
平台代码为 `deepseek-web`，来自用户人工登录后的真实官方页面，包含页面可见回答、联网状态和截图证据。管理员在设置中心启用后，它会进入后续新运行的全局监测范围；它不再是项目级默认选项。
_避免使用_：DeepSeek API、API 失败后的备用结果

**豆包 API 样本**：
平台代码为 `doubao`，来自正式 API Adapter。它不代表用户在豆包官方网页中看到的页面结果。
_避免使用_：豆包 Web、网页版样本

**豆包 Web 样本**：
平台代码为 `doubao-web`，来自人工登录后的豆包官方网页。管理员在设置中心启用后，它会进入后续新运行的全局监测范围；启用不等于登录有效，正式运行仍以专用 Chrome 登录验证和实际采集结果为准。
_避免使用_：豆包 API、API 失败后的备用结果

**单品牌工作台**：
正式产品只使用系统默认品牌。管理员在设置中心维护品牌资料、自动监测和竞品；旧 `/geo/projects` 只跳转到该入口。默认品牌不可归档或删除，品牌资料修改不删除历史回答、引用或指标，只使可重新生成的报告快照失效。
_避免使用_：让用户新建多个品牌、把旧项目页写成正式入口、修改品牌资料后重算或删除历史

**网页版配置状态**：
设置中心展示专用 Chrome 是否可用、Profile 是否已初始化，以及本次后端进程是否已验证网页登录。Profile 已初始化不等于当前登录仍有效；只有“网页登录已验证”表示本次检查通过。系统不识别或展示具体网页账号身份。
_避免使用_：把固定 URL/模型配置、Profile 目录存在或平台已启用称为“已经登录”

**运行前网页登录检查**：
系统对本次全局启用平台执行实时可用性检查。Web 平台未登录、需要人工验证或页面状态不确定时，仅跳过该平台并记录明确原因；其他可用 Web 或 API 平台继续运行。全部平台均不可用时不创建记录、不消费配额。
_避免使用_：一个 Web 平台失败就阻断整批、静默吞掉跳过原因、自动把 Web 结果替换为同名 API

**重试前网页登录检查**：
会再次向 Web 页面发送问题的失败项重试，只检查原失败记录的平台当前是否可用；不可用项保留并列为跳过，不阻止同批其他原平台重试。`analysis-only` 重试复用完整原回答，不访问 Web 页面。重试不读取品牌或问题的遗留平台字段，也不追加原运行之后新启用的平台。
_避免使用_：分析重试重新发送页面问题、用无关 Web 平台阻止 API 重试、按当前全局范围扩写旧运行

**自动监测网页登录检查**：
自动监测与手动运行使用同一全局平台范围和跳过语义。不可用平台进入本次运行的跳过快照，其他可用平台继续执行；配额和记录数只计算实际可执行目标。
_避免使用_：自动监测另设项目平台范围、静默跳过未登录平台、按遗留字段筛选

**自动监测人工补跑**：
自动监测因 Web 平台未登录而失败后，下一次执行时间仍推进到下一个计划时点，不自动重试；管理员完成登录与验证后，通过手动运行补回本次监测。
_避免使用_：循环重试登录错误、登录后自动发送、无限等待人工操作

**自动监测失败状态**：
项目列表展示最近一次自动监测计划的结果、具体失败平台与原因、设置入口和下一次计划时间；网页登录失败不创建空白运行报告，登录恢复后由用户从问题库手动补跑。
_避免使用_：只显示执行失败、用空报告表示登录错误、隐藏下一次执行时间

**当前 API 预置默认值**：
全新初始化时，豆包 Web、DeepSeek Web、豆包 API、DeepSeek API、千问 API 和腾讯混元 API 只作为预置写入，启用状态全部为关闭，必须由管理员主动启用；设置页固定按上述 Web 优先顺序展示，自定义平台排在预置之后。已有数据库继续保留管理员保存的启停状态。DeepSeek API 平台 `deepseek` 默认模型为 `deepseek-v4-pro`；正式结构化分析不设置应用层输入或输出 Token 上限，DeepSeek 思考模式默认关闭，管理员修改分析专用请求参数时必须二次确认。千问 `qwen` 使用 Responses 协议并预置强制搜索参数，腾讯混元 `hunyuan` 使用 TokenHub Chat `hy3-preview` 并预置 `web_search_options.enable=true`；这些请求参数不会令停用平台自动启用。只有供应商实际返回 `search_results` 才算联网已验证，账号还必须在 TokenHub“工具管理”具备可用的联网搜索资源。API 平台与同品牌 Web 平台仍是独立样本。
_避免使用_：把 DeepSeek API 停用理解为 DeepSeek 网页版停用

**全局启用平台范围**：
设置中心的启用状态是新监测运行唯一的平台范围来源；全部启用问题会在全部全局启用且具备监测能力的平台上运行。项目和问题的历史 `platforms` 字段仅用于兼容旧数据，不参与新计划，也不再由正式 UI 写入。基础配置不完整的平台不能启用；清除 API Key 会同步停用平台。结构化分析 API 仍独立选择，不会自动加入监测范围。
_避免使用_：品牌级平台选择、问题级平台选择、按遗留字段缩小新运行、静默切换分析 API

**API 联网能力状态**：
管理员主动检测 API 响应中是否存在可验证的联网搜索证据；该状态用于说明数据质量，不决定平台能否加入项目。只有检测成功可以称为“联网已验证”，未检测、未确认或失败的平台仍可由用户明确选择运行。
_避免使用_：把连接成功称为联网成功、把强制搜索参数称为已执行搜索、用联网状态自动停用平台

**引用**：
当前回答正文中的引用标记、与当前回答关联的来源卡片或当前回答展示的 HTTP/HTTPS 链接。只有这类平台引用进入引用 KPI。
_避免使用_：把所有页面检索结果都称为引用

**检索候选**：
页面自身检索链路中出现、但没有被当前回答引用的来源。它可以用于复核，不进入引用 KPI。
_避免使用_：引用、模型推测来源

**Web 证据**：
记录归属的联网状态截图、最终回答截图和有界采集元数据。数据库只保存不透明 artifact 标识，不保存服务器绝对路径。
_避免使用_：浏览器 profile、Cookie、Authorization、完整网络响应

**无回退**：
`deepseek-web` 或 `doubao-web` 失败时记录对应 Web 错误，分别不调用 `deepseek` 或 `doubao` 生成替代回答。API 与 Web 永远是独立样本。
_避免使用_：自动降级、备用 API 回答

## GEO 指标语言

**GEO 报告**：
基于 GEO 监测回答、引用与指标形成的项目或单次运行报告。它只表达 GEO 监测结果，不包含营销数据 AI 分析的结论或图表。
_避免使用_：营销数据 AI 分析报告、通用 AI 报告

**GEO AI 结构化分析**：
对单条 GEO 监测问题的完整 AI 回答进行证据化结构提取，并形成品牌提及、竞品关系、候选顺序与情绪等 GEO 指标输入。它不读取或解释营销数据。
_避免使用_：营销数据 AI 分析、通用 AI 分析

**当前正式分析契约**：
新建的单问题、问题集、自动监测和已有完整原回答的 analysis-only 重试统一使用 `ai_structured_v4` / `geo_metric_input_v4`，指标语义统一为 `contextual_competitor_mentions_sov_v1`。分析器接收当前问题、完整原回答和目标品牌，不接收项目竞品清单；输入超限、输出截断、证据或结构无效时整条分析失败，不回退 v3、旧分析器、旧 SOV、截断输入或分段计算。v3 及更早结果只按历史记录读取。
_避免使用_：把模块存在称为正式生效、把分析失败回答计入品牌指标

**品牌提及率**：
有效分析回答中，提到目标品牌的回答所占比例；同一回答无论重复提到目标品牌多少次，都只计为一条已提及回答。
_避免使用_：品牌提及次数、回答内竞品提及占比

**竞品**：
在当前问题和回答场景中，能够满足与目标品牌相同需求、可作为替代选择的品牌或公司。竞品是逐条回答中的场景关系，不是一个实体永久不变的全局身份。
_避免使用_：其他企业实体、永久竞品身份

**人工配置竞品**：
项目中预先维护的已知竞品名称和别名，用于向 AI 提供识别提示和业务背景。它不直接决定单条回答的竞品关系或 SOV 分母。
_避免使用_：强制竞品、永久竞品真值

**回答内竞品提及占比（SOV）**：
目标品牌有效提及次数占同一回答中“目标品牌与竞品”全部有效提及次数的比例。竞品由 AI 依据当前问题和回答语境识别，该指标不代表市场份额。
_避免使用_：市场份额、全部企业实体提及占比

**聚合 SOV**：
每条回答先独立计算 SOV，再对所有可计算回答做等权算术平均。目标品牌和竞品都未提及时为 `N/A` 并排除；分析失败也排除。不得把全部回答的品牌与竞品提及次数汇总后相除。
_避免使用_：池化提及次数、把 `N/A` 或分析失败当成 0%

**分析覆盖率**：
成功生成新版品牌指标的回答数，占已经采集到完整原回答的回答数。分析失败只降低覆盖率；没有采集到原回答的监测失败不进入覆盖率分母。
_避免使用_：有效样本率、把采集失败和分析失败混为一谈

**版本化历史**：
新记录、新报告和新导出显示回答内竞品提及占比；迁移前的旧记录与旧报告继续显示原 `share_of_voice` 值和“历史竞品配置口径”名称。新旧值不得共同平均、共同画成连续趋势或互相回填。
_避免使用_：用新公式重算历史、无版本混合趋势

**SOV 监测问题**：
用于比较目标品牌与竞品表现的非品牌词问题，不直接包含目标品牌名称或别名。
_避免使用_：目标品牌怎么样、目标品牌有哪些产品

**SOV 比较基线**：
用于项目看板和项目报告解释 SOV 变化的一组稳定监测问题。问题集合发生实质增删后，应从变更日重新观察趋势，不直接把变更前后的差异解释为品牌表现变化。项目级页面按所选时间范围展示实际产生的历史数据，默认合并全部平台，并支持切换到单个平台查看；当前平台配置只决定未来采集，不得隐藏历史数据。单次问题或单轮问题集报告只描述本次运行，不涉及跨期比较基线。
_避免使用_：混用不同问题集合的连续趋势

## 营销监控语言

> 阶段说明：以下术语是 2026-08-03 确认的现行长期业务口径，数据源权威和官网无侵入接入红线见 `docs/adr/0001-marketing-funnel-data-source-of-truth.md`。百度 OAuth、搜索账户绑定、最近 30 个上海完整日搜索推广快照和百度统计读取已于 2026-08-04 部署并从正式入口 live verified。官网表单聚合与脱敏记录代码也已部署，并已用独立只读凭据完成本地真实对账，但生产因缺少专用只读项目与账号凭据仍为 `DISABLED`。2026-08-05 官网 `DISABLED` 会话级跳过和首页广告旧快照后台刷新已随 `98467f0` 推送到 GitHub；该提交尚未部署或从正式入口验收，当前生产 revision 仍保持既有同步等待行为。53KF 在线客服咨询、线索入池、成交订单数、成交订单金额和完整来源关系仍未完成端到端接入与生产验证。不得把“百度已上线”“官网代码已部署”或“提交测试通过”描述为已经打通完整营销漏斗或已经正式生效。

**营销监控系统**：
面向市场负责人的只读数据观察与分析入口，汇总来源系统中的营销事实，用于判断渠道、活动或项目的投入与结果；业务调整仍在对应来源系统中完成。
_避免使用_：广告投放系统、销售作业系统、CRM

**营销数据 AI 分析（前端预览实施中，后端规划中）**：
用户针对一个监控项目和明确分析周期主动生成固定营销分析报告的过程。应用重新读取首版已接入来源，由程序计算权威指标并构建有界 JSON 证据包，再由模型一次生成结构化报告；结构无效时最多纠正一次。首版不提供自定义分析重点、Agent、模型工具调用、自由问数、持续对话或会话记忆，也不读取 GEO 报告或 GEO AI 结构化分析结果。当前正在实施诚实的页面结构预览；真实来源读取、模型生成与报告持久化尚未实现。
_避免使用_：GEO AI 结构化分析、Agent 分析、模型现场调用接口、与数据持续聊天、实时看板、已经上线的真实营销 AI 报告

**营销数据 AI 分析模型**：
只负责根据应用提供的固定证据包组织摘要、解释、待验证假设和建议，并声明需要哪些受控图表；它不负责取数、核对接口、计算权威指标或证明跨来源因果。模型可以复用已有供应商凭据，但平台、模型和请求参数与 GEO AI 结构化分析独立配置。
_避免使用_：营销数据查询 Agent、GEO AI 结构化分析模型、共享且联动的分析配置

**营销数据 AI 分析运行**：
用户发起一次营销数据 AI 分析后形成的持久过程记录，覆盖来源读取、证据包锁定、结构化模型生成与结果校验。成功运行只产生一份不可变报告；失败或取消运行不产生报告。用户离开页面不终止运行，同一项目不允许同时运行两份。
_避免使用_：营销数据 AI 分析报告、同步页面请求、失败报告、并行双跑

**营销数据 AI 分析调用记录**：
一次模型生成或结构纠正尝试的轻量审计信息，只记录阶段、模型、版本、耗时、Token 用量和归一化结果或错误。首版不保存完整 Prompt、模型原始响应、上游原始报文或聊天历史。
_避免使用_：完整会话、原始营销数据副本、模型原文归档

**运行重试**：
自动或人工继续同一条失败的营销数据 AI 分析运行，保留同一运行标识、原始周期、配置版本和历史调用尝试。分析数据基线尚未锁定时，重试丢弃未提交的部分结果并完整重读首版来源；基线已经锁定后，重试复用原证据包，只继续未成功阶段。项目创建更新运行后，旧失败运行不再可重试。
_避免使用_：重新运行、重新生成报告、新数据基线

**重新运行**：
用户以相同或调整后的分析周期重新发起营销数据 AI 分析，创建新的运行、重新读取来源并确定新的数据基线。新运行成功后形成一份新报告，不改写既有成功报告；新运行失败、取消或仍在进行时，当前报告保持为最近一份成功且未删除的报告。
_避免使用_：运行重试、复活失败运行、覆盖旧报告

**取消运行**：
用户主动终止一条尚未完成的营销数据 AI 分析运行；已经发出但无法立即中断的外部调用结果必须丢弃，运行不再进入后续阶段，也不产生报告。取消是不可重试的终态，再次分析必须重新运行。
_避免使用_：运行失败、暂停运行、取消后继续执行

**可选分析周期**：
首版生成营销数据 AI 分析报告时默认选择最近 30 个已经结束的上海自然日，用户可以选择最长 90 个自然日的闭区间，截止日不得晚于报告生成日前一个上海自然日；生成时读取所选周期及紧邻的等长上一周期，不受主看板当前 30 日快照限制，也不建设长期营销历史仓库。报告生成后永久保留当次周期、结论、图表与证据。
_避免使用_：当天未完整数据、只能分析主看板当前快照、报告只保留 30 天、长期营销历史仓库

**首版营销分析来源**：
百度搜索推广四份独立报告、百度统计和官网表单咨询。百度推广四份报告在一次读取中全成全败；53KF 在线客服、线索和订单在尚未接入时只作为覆盖缺口，不由同期数据补齐。每次新运行都重新读取来源，不使用旧报告、主看板缓存或失败回退作为本次证据。
_避免使用_：模型按需选来源、旧缓存兜底、把未接入来源表示为零

**分析数据基线**：
一次运行中锁定且保持不变的来源事实集合，包含所选周期、紧邻的等长上一周期、各来源实际覆盖与状态。来源读取或质量校验有问题时，由应用标记失败或不可比较，模型不得再调用接口核对；重新运行才会确定新基线。
_避免使用_：分析过程中持续刷新的实时数据、模型二次取数、混合多个时间截面的报告

**营销分析证据包**：
应用从已校验来源事实构建的有界、版本化 JSON，包含程序计算好的指标、趋势、排名、数据覆盖、比较资格、有限脱敏样本和图表可引用数据集。模型只能解释证据包，不得补造其中没有的数字。
_避免使用_：完整上游响应、长期原始营销数据仓库、任意接口结果拼接

**可比较周期**：
同一来源的本期与紧邻等长上一周期均完整、口径一致时，才允许计算和展示变化。缺失、失败、部分覆盖或口径不一致时显示不可比较，不把缺失当零。
_避免使用_：跨来源环比、用部分覆盖数据推算完整变化

**营销搜索词 AI 样本**：
从完整搜索词事实中按确定性规则选出的有界文本样本，进入证据包前清理控制字符、限制长度并遮蔽明显手机号、邮箱和身份证号，不包含内部 ID；报告必须说明结论只覆盖有限样本，完整原始搜索词仍留在来源模块本地。
_避免使用_：完整搜索词清单、联系人信息、无边界原文输入

**营销结构化报告生成**：
将完整证据包一次性交给营销分析模型并要求返回固定 JSON 的过程；若核心结构无效，最多使用同一基线纠正一次。首版模型请求显式关闭联网和工具，来源核对、数值计算及格式校验都由应用完成。
_避免使用_：多步 Agent 工作流、模型调用业务 API、无限纠错

**营销数据 AI 分析报告**：
一次营销数据 AI 分析成功完成后形成的不可变结果，包含当次结论、图表、最小数据证据与分析数据覆盖。报告可以只覆盖部分来源，但不得把缺失或失败来源表示为零，也不得用覆盖不一致的数据计算跨来源比率或归因关系；生成失败只构成失败的分析运行，不产生报告。
_避免使用_：GEO 报告、实时查询结果、失败报告

**营销数据 AI 分析报告结构**：
首版报告使用固定骨架，依次表达报告身份与周期、分析数据覆盖、核心摘要、关键指标、带证据和图表的洞察、营销分析建议及口径限制。AI 只能填充约定内容和有限洞察块，不能创建任意页面组件、自由布局或自定义章节类型。
_避免使用_：AI 生成页面、任意 Markdown 报告、自定义组件树

**营销数据 AI 分析报告保留**：
成功报告默认长期保留，不自动过期、不被重新运行覆盖，也不能编辑其中的结论、图表或数据。项目所有者或管理员可以明确删除整份报告及其固定数据证据，但系统继续保留最小删除审计；停用用户不触发报告删除。
_避免使用_：绝对不可删除、自动过期、停用用户时级联删除、修改历史报告

**当前营销数据 AI 分析报告**：
项目最近一份成功且未删除的报告。新的运行只有成功提交报告后才替换当前报告；失败、取消或进行中的运行不改变当前报告。删除当前报告后，系统回退到最近一份仍保留的成功报告。
_避免使用_：最新运行、失败报告、运行中草稿

**营销数据 AI 分析报告历史**：
项目中所有成功且未删除报告的站内列表；每次重新运行成功后增加一份，首版不提供报告间自动比较。
_避免使用_：运行日志、失败任务列表、报告对比功能

**营销数据 AI 分析报告可见性**：
首版报告只供项目所有者和管理员在登录后的站内页面查看，不提供公开链接、匿名访问、邮件发送或文件导出。查看权限始终跟随监控项目权限，拥有报告标识本身不构成访问授权。
_避免使用_：公开分享链接、匿名报告、仅凭报告 ID 访问

**营销分析结论**：
对程序依据既有指标口径计算且可以定位到报告数据证据的营销事实所作的解释。AI 可以选择和组织结论，但不得自行定义公式、生成未经计算的数值，或把同期变化表述为因果关系。
_避免使用_：无证据判断、AI 现场计算指标、因果证明

**营销分析建议**：
依据营销分析结论提出的后续核查或业务行动方向，不属于已经被数据证明的事实。报告必须把建议与事实结论明确区分。
_避免使用_：分析结论、已证明的原因、自动执行的业务动作

**图表意图**：
营销数据 AI 分析报告中对图表类型、标题、数据集引用、字段与单位的版本化声明，用于把当次固定数据证据呈现为图表。图表意图不包含图表库原生配置、可执行代码、回调、HTML 或样式脚本。
_避免使用_：Ant Design Plots 配置、ECharts 配置、AI 生成前端代码

**分析数据覆盖**：
营销数据 AI 分析报告生成时，各营销来源在所选周期内的可用、未接入、无数据或读取失败状态。至少一个来源存在可分析事实时允许形成部分覆盖报告；全部来源都没有可分析事实时不产生报告。
_避免使用_：把未接入或读取失败表示为零、完整营销漏斗、用同期数据补齐缺失来源

**监控项目**：
以一个品牌或业务线为边界的长期观察单元，可以关联该范围内的多个广告账户与活动、落地页和销售业务范围。
_避免使用_：单次广告活动、单个推广计划

**付费广告表现**：
百度营销中付费广告产生的展现、点击和消费等投放事实，以百度营销数据为准；它只描述付费投放，不代表官网全部访问。
_避免使用_：官网总流量、自然访问量

**原始咨询**：
客户通过百度广告或自然访问进入官网落地页后，经表单或咨询入口留下、尚未经市场人员确认有效性的联系记录；原始咨询可能经市场确认后进入销售系统线索池，也可能不再继续。
_避免使用_：有效线索、销售机会、成交客户

**表单咨询**：
官网数据库中已经成功写入的表单提交。`submission_sessions` 表示带会话归因的表单提交会话数；它不覆盖 53KF 在线对话，也不等于全部客服咨询、有效线索或成交。
_避免使用_：联系入口点击、在线客服咨询、有效线索

**在线客服咨询**：
53KF 中访客实际发送过消息的有效对话。仅打开客服窗口、加载客服脚本、点击浮窗或收到机器人自动问候不构成咨询。
_避免使用_：客服窗口打开次数、自动问候次数、联系入口点击

**官网一方统计**：
官网为表单来源归属和数据质量核验保存的会话、来源、落地页、联系入口点击和提交上下文。它是记录级归因与对账证据，不是网站来源、访问、UV 或 PV 的主数据源；`contact_click_sessions` 只表示联系入口点击会话。
_避免使用_：百度统计替代品、客服咨询数、把官网与百度统计流量相加

**有效线索**：
市场或客服人员确认原始咨询后，手动转换并放入内部销售系统线索池形成的销售线索。有效线索已经通过初筛但尚未成交；首页以“线索入池数”监控这一必要链路阶段。
_避免使用_：原始咨询、销售机会、把所有落地页表单都计为有效线索

**销售机会**：
销售人员从线索池承接有效线索并进一步沟通，确认客户具有进一步合作意向后形成的正式机会。销售机会不等于订单或成交，只解释业务过程，不是必须同步的监控指标。
_避免使用_：有效线索、有效商机、订单、成交

**订单**：
销售沟通完成并确认合作后，在内部销售系统中建立的成交业务记录。未来接入时必须提供用于去重和关联的最小稳定标识、成交订单数和成交订单金额；无需为首页同步完整订单对象或其他销售过程字段。
_避免使用_：有效线索、销售机会、询价记录、把完整订单对象作为必接数据

**订单签订金额**：
订单签订时在内部销售系统中记录的确认金额，用于计算 ROAS 并观察营销投入带来的签订结果；它与成交订单数都属于必须同步的结果指标，但不代表实际回款、营业收入或毛利。
_避免使用_：回款金额、营业收入、毛利、销售机会金额

**成交订单数**：
在统计周期内、满足当前来源关系与成交状态口径的去重订单数量。CPA 使用成交订单数作为分母；成交率和整体转化率使用成交订单数作为分子。不得用订单金额或其他记录数反推。
_避免使用_：订单金额、机会数、未成交订单数、用金额估算订单数

**后续联合观察目标**：
当前真实数据试点监控百度搜索广告的展现、点击和消费，并单独读取百度统计的 PV、访问次数和访客数；本地工作区另接入官网表单咨询。目标首页按来源展示“展现 → 访问（点击） → 官网表单咨询 / 在线客服咨询 → 线索入池 → 成交订单”，成交订单同时展示订单数和订单金额，官网表单与 53KF 在线客服不得静默合计。2026-08-03 已通过百度统计 `source/all/a`、`source/engine/a` 和 `clientDevice=pc/mobile` 的真实响应确认：站内非付费访问可安全拆为百度搜索、必应搜索、直接访问和其他来源；其他来源包含非百度/必应搜索与外部链接，百度付费推广必须排除。必应在所选范围无报告行时显示真实 0。只有可信来源键或经确认的人工映射成立后，跨系统记录才能进入同一来源行；仅仅同期出现不得表达为归因事实。
_避免使用_：把全站流量直接归因到广告、把同期数据拼成漏斗、把尚未接入的数据描述为当前已监控事实

**营销数据主数据源**：
百度营销负责广告展现、广告点击和消费；百度统计负责网站来源、访问次数、UV 和 PV；官网成功表单负责表单咨询及其记录级来源关系；53KF 负责在线客服有效对话；内部销售系统负责线索入池和成交订单。多套流量不得相加、平均、互补或择优采用。
_避免使用_：双主数据源、流量补差、用官网 PV 替换百度统计

**全链路市场数据监控**：
系统的长期目标是在同一监控项目中按来源联合观察展现、访问（点击）、客服咨询、线索入池、成交订单数和成交订单金额。它描述跨来源系统的只读观察能力，不表示系统代替百度营销、官网、客服或销售系统进行业务操作，也不表示当前阶段已经全部接入。
_避免使用_：全链路业务系统、已经打通、用试点数据代表完整营销漏斗

**只读监控**：
系统可以从来源系统同步、保存和计算监控数据，但不修改来源系统中的广告、咨询、线索、机会或订单记录；需要操作时通过链接进入来源系统。
_避免使用_：双向同步、跨系统代操作

**来源系统**：
负责产生并维护某类业务事实的原始系统；营销监控系统引用这些事实，不反向成为该事实的维护入口。
_避免使用_：把营销监控系统称为所有业务数据的唯一录入系统

**人工关联**：
当来源系统缺少统一来源键时，用户确认不同来源系统记录之间的来源关系；该关系只由营销监控系统保存，不修改或回写来源系统。人工关联是可信来源关系的一种补充方式，不等同于自动多触点归因。
_避免使用_：来源系统打通、自动身份匹配、双向同步

**主要归因咨询**：
缺少统一来源键时，每笔成交订单由用户借助最小订单标识选择一条主要客服咨询，作为该订单数和订单金额的来源关系。销售系统仍需提供线索入池阶段与成交订单结果所需的最小事实，不需要同步完整销售机会或订单过程对象。
_避免使用_：未经确认的自动归因、把同一订单重复计入多个来源、要求完整销售对象进入监控系统

**访问触发式十分钟数据库缓存**：
没有用户访问营销入口时，系统不定时轮询百度。访问广告看板时先检查数据库快照；快照缺失或距最近成功刷新超过 10 分钟，才创建 `ON_DEMAND` 运行并合并同项目并发请求。当前生产 revision 在快照过期时仍同步等待刷新完成；已推送的提交 `98467f0` 改为有过期旧快照时立即返回旧快照并后台刷新，响应携带 `activeRun`，前端轮询刷新运行、成功后静默重拉新数据。该提交尚未部署，正式行为切换必须以生产 revision 和入口验收为准。无快照的首载继续同步等待；失败且已有历史快照时保留最后成功快照。百度统计按项目、站点、设备和覆盖范围缓存基础汇总；首页首次只读取全站趋势与来源汇总，用户选择某个来源后才读取并独立缓存该来源趋势。页面不展示“数据陈旧”或更新时间，诊断状态仍保留在接口元数据和运行记录中。当前单实例、低频内部使用不引入 Redis；数据库唯一约束与进程内 single-flight 分别负责持久复用和同进程请求合并。
_避免使用_：把缓存描述为无人使用时仍固定刷新、把未选择的全部来源趋势描述为预取、把每次日期筛选描述为必然调用百度 API、把过期读取描述为始终同步阻塞等待百度

**百度搜索广告数据粒度**：
现役实现读取并原子保存推广计划、推广单元、投放关键词和搜索词四份官方报告；广告表现的严格结构下钻为“项目 → 推广计划 → 推广单元 → 投放关键词”。搜索词报告不提供关键词 ID，因此搜索词独立展示，只稳定关联到账户、推广计划和推广单元，并保留上游返回的关键词名称证据。2026-08-04 已部署最近 30 个上海完整日、双读一致性与共享资源预算实现，并从正式入口验证关键词、广告表现和网站流量真实数据；创意层级仍未接入。
_避免使用_：从计划汇总推断下级数据、把搜索词当投放关键词、为搜索词伪造关键词 ID、把本地实现描述为生产已生效

## 用户生命周期语言

**停用用户**：
管理员停止用户继续登录和使用系统，但保留该用户已有的项目、报告、监测记录、配额记录和审计历史。停用是日常用户管理操作，可以由管理员重新启用；内部状态统一使用 `inactive`。
_避免使用_：删除用户、注销数据、`disabled`

**永久删除用户**：
不可逆地处理用户身份及其历史数据的独立高风险操作。当前用户管理不提供永久删除；后续只有在数据保留、匿名化、关联记录和文件证据规则全部明确后才能单独设计。
_避免使用_：把停用按钮或 `DELETE /api/users/:id` 描述为永久删除

## SEO 检测语言

**Sitemap 页面清单**：
网站通过 Sitemap 主动声明的页面集合，用于帮助搜索引擎发现页面，不代表允许抓取或允许索引。
_避免使用_：Sitemap 权限、允许页面清单

**Sitemap 可用性**：
Sitemap 是否包含可被识别的有效页面地址或子 Sitemap 地址。
_避免使用_：Sitemap 页面覆盖

**Sitemap 页面覆盖**：
有效 Sitemap 页面清单与已知可索引页面之间的收录差异，以及清单内需要清理的页面。
_避免使用_：Sitemap 可用性、Sitemap 格式

**可抓取内部入口**：
站内其他页面通过带有效 `href` 的链接指向目标页面所形成的入口。
_避免使用_：可点击入口、JavaScript 跳转入口

**可抓取导航链接**：
用于跳转到另一个页面的导航入口，并通过带有效 `href` 的 `<a>` 表达目标地址。
_避免使用_：把所有可点击的 `span`、`div` 都视为导航链接

**菜单触发器**：
只负责展开或收起菜单、不负责跳转页面的界面控件。它不属于 SEO 导航链接问题；语义与键盘操作问题归入无障碍检测。
_避免使用_：无效导航链接

**交互后出现的链接**：
页面初始 DOM 中不存在，只有悬浮、点击或聚焦后才被创建的链接。它与菜单触发器使用什么标签是两个独立问题。
_避免使用_：悬浮按钮、无效导航

**导航问题判定证据**：
只有检测到缺少有效 `href` 的 `<a>`、能够明确识别页面跳转行为的 `span`/`div`，或只在交互后创建的链接时，才报告 SEO 导航问题。仅有点击样式、`cursor-pointer`、通用点击事件等模糊特征时，不判定为 SEO 错误。
_避免使用_：疑似可点击元素、可能无法抓取

**疑似孤儿页面**：
已知页面中，没有任何可抓取内部入口指向的页面。
_避免使用_：Sitemap 缺失页面、导航标签错误页面

**疑似孤儿页面检查前提**：
必须先获得有效 Sitemap 或其他独立页面清单，才能判断哪些已知页面没有可抓取内部入口。缺少独立页面清单时，结果应为“暂时无法检查”，不得显示“通过”或“0 个孤儿页面”。
_避免使用_：Sitemap 不可用时未发现孤儿页面

**检测口径生效范围**：
检测结果保留生成时的事实与口径，不追溯改写历史报告。检测规则调整后，通过重新检测生成新报告进行验收；旧报告可标注为旧版检测结果。
_避免使用_：自动修正历史检测数据

**抓取与索引权限**：
由 robots 规则和索引指令表达的搜索引擎访问与收录限制。
_避免使用_：Sitemap 权限
