# 项目级 AI 工作约定

## 当前生产真值

- 当前唯一支持的正式访问入口是 `https://insight.guangtuo.com`。
- `insight.gato.com.cn` 是已退役的历史域名；除非是在带日期的历史验收记录中，不得把它写成当前入口。
- `http://182.254.140.163/` 命中 Nginx 默认站点，不是本应用。不得用直接访问服务器 IP 是否出现应用页面来判断服务是否部署成功；HTTPS 直连 IP 也不是受支持入口。
- 当前实例的环境、Nginx、证书、百度 callback 和验证口径以 `docs/DEPLOYMENT.md` 的“当前正式单机实例”一节为准。回答生产状态前应重新核对运行时，不得仅凭文档日期推断实时状态。
- 域名、证书或前端构建切换不等于服务器 Git 源码已经更新。判断“服务器是否最新”必须分别比较服务器 `HEAD`、`origin/main` 和工作区状态；带日期的历史版本与验收证据统一见 `docs/DEPLOYMENT.md`。

## 部署与图形会话边界

- 应用代码先在本地修改、测试、提交并推送，再通过经校验的 Git Bundle 快进服务器 `main` 并执行仓库正式部署入口；`git pull --ff-only` 只保留为服务器可稳定访问 GitHub 时的兼容方式。不得直接编辑服务器项目源码。
- 临时发布锁已经解除，正式发布统一使用 Git Bundle 流程。当前 workflow、公开 revision、服务器 `HEAD` / `origin/main` / 工作区关系和带时间验收证据只在 `docs/DEPLOYMENT.md` 维护；回答生产状态或执行发布前必须重新核对，不得把服务器旧的远端跟踪引用误作运行真值。
- Ubuntu 正式前后端由 `ai-geo-backend.service` 和 `ai-geo-frontend.service` 托管。不得通过 SSH、远程桌面、`nohup`、PM2 或第二套 Node 命令并行启动后端。
- DeepSeek Web 和豆包 Web 的专用 Chrome 需要服务器上持续存在的图形桌面会话。远程桌面客户端可以断开，但不能注销、结束或销毁该桌面会话；这不表示后端要从桌面里启动。
- `DISPLAY` 指定 X 图形显示会话，`XAUTHORITY` 指定访问该会话的授权文件。SSH 终端里的临时 `export` 只影响该终端启动的子进程，不能替代正式 systemd 配置。
- 服务器 `.env`、数据库、Token、浏览器 Profile 和证据目录是持久运行数据，不得复制到 Git。百度生产 Token 必须保留在服务器数据库密文中；本地开发使用脱敏 fixture。

## 百度回调

- 服务器期望的完整 callback 是 `https://insight.guangtuo.com/api/admin/marketing/baidu/oauth/callback`。
- 百度开发者控制台也必须登记完全相同的地址。仅修改服务器环境变量不能替代控制台配置；在控制台更新得到人工确认前，不得声称新域名重新授权已经可用。
- 现有服务器 Token 不因域名切换而删除。除非撤权、过期且刷新失败或确认泄露，不得为了测试主动清除生产 Token。

## 百度搜索推广层级合同

- 本地现役百度广告读取路径固定调用四份互相独立的官方报告：推广计划 `2290316`、推广单元 `2284618`、关键词 `2602783`、搜索词 `2307838`。不得再使用只返回推广计划的 provider 作为正式 fallback，也不得从计划汇总反推单元、关键词或搜索词。
- 严格广告结构只到“账户 → 推广计划 → 推广单元 → 关键词”。搜索词报告没有关键词 ID，只有计划 ID、单元 ID、关键词名称和搜索词；因此搜索词是独立分析事实，只能稳定关联到推广单元并保留关键词名称证据，禁止伪造 `keywordId` 或把搜索词嵌成关键词 ID 的严格子节点。
- 四份报告必须在同一次项目刷新中全成全败，并写入同一个 `refresh_run_id`。任一报告失败、超预算、出现重复事实或父子 ID/名称不一致时，不得覆盖上一份完整快照。
- `/api/marketing/projects/:projectId/dashboard` 的百度广告合同分别返回 `campaigns`、`adGroups`、`keywords`、`searchTerms`；官网表单继续只走 `/api/website-data`，百度统计访问继续使用营销模块内独立的 Tongji 服务，三者不得混写字段或表。
- 2026-08-04 正式版本已使用最近 30 个上海完整日双读计划、单元、关键词和搜索词四份官方报告，并以同一刷新事务原子落库；生产登录后页面已显示截至 2026-08-03 的真实广告快照，关键词页为 863 条且不再出现“广告关键词数据尚未开放”。仍须区分“真实账号已观测”与“特定日期范围页面聚合”，不得把 2026-08-03 单次上游样本行数当成所有页面的固定数量。

## 百度统计与百度营销重叠数据合同

- 百度营销与百度统计必须同时保留，不得互相替代。百度营销负责回答“广告投给谁、展现/点击/消费多少、广告对象与投放状态是什么”；百度统计负责回答“访问从哪里来、进入网站后看了什么、访问质量和站内行为如何”。完整裁决与 API 清单见 `docs/adr/0001-marketing-funnel-data-source-of-truth.md`。
- 百度统计“搜索推广”页面虽可按计划、单元、关键词、创意、搜索词和推广 URL 查看消费/点击与 PV、跳出率、访问时长等组合数据，但这是广告账户维度与网站维度的联表分析视图，不是完整广告对象合同。不得据此用百度统计替代百度营销的稳定 ID、层级关系、状态、出价、匹配、质量或投放事实。
- 展现、广告点击、消费、CTR、CPC 等在两套平台中出现时属于重复事实，只允许按同一账户、日期、时区、设备和覆盖范围进行对账；不得相加、平均、补差、择优取值或创建双主数据源。正式展示和存储仍以百度营销为准，百度统计副本只作诊断与一致性证据。
- 百度统计的访问次数、PV、UV、新访客、来源、设备、地域、入口页、受访页、跳出率、平均访问时长和平均访问页数仍以百度统计为唯一主数据源。广告点击不得冒充访问，百度推广平台的效果指标不得反推网站访问事实。
- 同名“转化”不是同一事实：百度统计转化依赖网站目标/事件配置，百度营销的目标转化、深度转化、线索或订单指标依赖推广转化追踪。未分别确认定义、归因窗口、去重、日期和覆盖前，必须分字段保存并显示缺失，不得按名称合并。
- 百度营销报告统一使用 `OpenApiReportService/getReportData` 并以 `reportType` 区分；百度统计报告统一使用 `ReportService/getData` 并以 `method` 区分。两套服务、Token、字段合同和统计粒度独立，不得混用凭据或把“同一 API 网关”误判为同一数据源。
- 网页可见维度不等于公共 API 已提供同等字段。新增百度统计推广层级、创意、搜索词或推广 URL 采集前，必须用官方文档和真实 API 响应逐项验证 method、维度、稳定键、分页、日期上限和数据口径；未验证时只能定义为候选诊断数据，不得进入正式合同。
- 2026-08-05 生产只读探针确认：`pro/product/a` 只有推广产品粒度；`custom/media/a` 的计划、单元、关键词和创意 flag 在当前公共 API/账户均返回 `910404`；`source/searchword/a` 可返回独立搜索词行为和 `is_promotion`，但其 `keywordId` 与同期百度营销关键词 ID 精确交集为 0。不得按统计侧 ID、关键词名称或搜索词文本把两套事实拼接；完整证据见 `docs/solutions/2026-08-05-baidu-tongji-promotion-api-capability.md`。
- `source=searchBaiduPro` 在当前 `source/searchword/a` 样本中没有改变响应，不能假设上游筛选已生效；所有新增过滤条件必须用正反样本证明响应确实变化。百度统计返回的 `--` 一律保持缺失，不得转为零。

## 营销页面读取 API 治理

- `docs/active-2026-08-05-006-marketing-api-resourceization/` 是广告页面读 API 最小化的唯一需求归属。不得另建 `dashboard?view=...`、页面 BFF 或第二套资源化需求与其竞争；市场总览、广告表现、关键词和搜索词的消费者清单以现役逐页矩阵和 006 为准。
- 006 的 GoodieAI 广告读取接口必须以一份 OpenAPI 3.1 文件作为唯一机器可读合同；`docs/API.md` 只保留面向人的现役摘要，不复制字段表。合同必须标明请求、响应、空值、错误、缓存、数据源和上游调用行为；前端 wire type 由该合同生成，后端用合同测试校验实际响应。在 006 交付该文件前，不得声称仓库已有正式 OpenAPI 合同。
- 百度上游 API 不写入 GoodieAI OpenAPI，也不复制整套官方文档。`backend/modules/marketing/contracts/baidu/` 的版本化 manifest 是实际消费端点、报告编号/方法、字段、官方来源、验证日期、能力状态和预算的唯一机器真值；005 只审计补漏、保持脱敏 fixture/合同测试并拆分 Provider，不建第二套手写上游文档或实时漂移平台。
- 当前生产已部署 006 R1 additive：市场总览仍读取返回 `campaigns`、`adGroups`、`keywords`、`searchTerms` 的完整 Dashboard，广告表现、关键词和搜索词详情页已分别使用 `/ad-hierarchy`、`/keywords`、`/search-terms` 并钉扎同一 revision。未完成 R2 前不得把轻量 Dashboard、旧数组和旧兼容实现描述为已经正式硬切或退役。
- 页面只能调用 GoodieAI 内部 API，不得从浏览器直接调用百度。详情资源只读取已落库完整快照并钉扎同一 `revision`，不得各自触发百度上游请求；快照过期检查和刷新仍由唯一后端协调路径合并，同一项目请求突发不得按页面数重复刷新四份报告。
- API 治理的验收顺序固定为：口径正确 → 不重复调用上游 → 页面最小返回体。接口命名或抽象整洁不能压过市场负责人看到正确、可解释数据的目标。
- 003 统一 OAuth、006 广告读 API 资源化、007 数据正确性和 005 Provider 模块化是不同事实面。006 的代码与 003 没有强技术依赖，但生产发布/观察窗口不得重叠；007 的广告双周期依赖 006，百度统计来源分区和页面消歧不依赖 006；005 必须在正确性工作关闭后最后实施。

## 市场首页与视觉标准

- 全局视觉设计与市场首页的现行权威规范是 `docs/visual-design-spec.md`；不得在单个需求目录复制第二份“全局标准”。
- 2026-08-04 确认的目标首页按内置渠道展示“展现 → 百度统计访问 → 官网表单咨询 / 在线客服咨询 → 线索入池 → 成交订单”，成交订单同时显示订单数和订单金额；官网表单与在线客服是独立事实。百度推广的广告投入/展现来自百度营销，访问来自百度统计；广告点击不得冒充访问。
- `CPA = 广告投入 ÷ 成交订单数`，`成交率 = 成交订单数 ÷ 线索入池数`，`整体转化率 = 成交订单数 ÷ 展现数`；缺失真实分母时显示缺失状态，不得用金额或其他数量反推。
- 只有可信来源键或经确认的人工映射成立时，记录才进入对应来源链路；不得把同期数据直接拼成归因事实。
- `nextjs-frontend/src/app/geo/market-overview/page.tsx` 已完成 2026-08-03 设计稿对应的视觉、交互、状态与响应式实现。现役市场页面共用设备优先的日期筛选器、周期比较指标卡和统一页面标题；生产可展示百度营销与百度统计真实数据。独立官网数据代码也已部署，但生产模块因缺少专用官网只读凭据而保持 `DISABLED`。官网表单统计覆盖所选区间全部成功入库表单记录，来源缺失仍计入总数并归为 `UNKNOWN`；它不包含 53KF。线索入池、成交订单数/金额和依赖指标仍是诚实缺失状态。
- 2026-08-04 逐页实施与生产状态统一见 `docs/README.md` 的“当前前端页面实施状态”，视觉验收证据见 `docs/blocked-2026-07-31-001-market-monitoring-frontend-ia/design-qa.md`。市场总览、广告表现、关键词分析、网站流量、咨询数据（其中“原始咨询”仍是领域事实名称）和订单结果页面均已部署；百度营销、百度统计与既有 AI/GEO 数据已从正式入口验证。官网代码已部署但生产配置仍禁用，53KF 仍为独立 `NOT_CONNECTED`，订单结果仍无数据源。不得用非生产 fixture 推断真实数据已接通。

## 营销数据主数据源与官网接入红线

- 营销漏斗的数据源权威、指标语义和官网无侵入接入边界统一以 `docs/adr/0001-marketing-funnel-data-source-of-truth.md` 为准；后续需求、实现、测试和报告不得另设冲突口径。
- 百度营销是广告展现、广告点击和消费的唯一主数据源；百度统计是网站来源、访问次数、UV 和 PV 的唯一主数据源。官网一方访问数据只用于表单来源归属、会话关联和对账，不得替代百度统计。
- 官网成功写入数据库的表单提交只构成“表单咨询”；53KF 中访客实际发送过消息的有效对话才构成“在线客服咨询”。`contact_click_sessions` 只是联系入口点击，`submission_sessions` 只是表单提交会话，表单处理状态也不是线索、机会或订单状态。
- 官网记录仅有 `organic_search` 标签时不能证明具体搜索引擎；只有通过严格 URL 校验的原始 `referrer` 才能按主机名拆成百度、必应、Google、其他搜索或外部引荐，没有可信证据的记录进入 `UNKNOWN`。不得把百度统计与官网一方流量相加、平均、补差，也不得把同期广告、访问、咨询、线索和订单拼成归因事实。
- GoodieAI 首页和咨询统计只允许后端分页读取官网联系人列表，使用 `id` 去重、`createdAt` 归日和来源字段分类，并立即聚合为九键数量。姓名、电话、邮箱、IP、咨询内容、访客/会话标识和原始 URL 不得进入统计快照、日志或公开统计响应；凭据必须由服务端环境或密钥管理注入，浏览器不得接收官网后台凭据或 JWT。
- 官网数据与百度数据必须保持独立边界：百度营销/百度统计继续使用 `backend/modules/marketing` 和 `/api/marketing`；官网表单统计固定使用 `backend/modules/websiteFormConsultations` 和 `/api/website-data`；按需脱敏咨询记录使用 `backend/modules/consultationRecords` 和 `/api/consultations`。三者不得共用客户端、服务、返回合同、模块状态或迁移账本，只共享系统登录鉴权和项目所有权校验等通用基础设施。
- 官网逐日表单固定使用 `/api/website-data/projects/:projectId/form-consultation-days`，与区间接口基于同一批联系人记录构建，单次最多连续 31 日；响应固定为 `dataCoverage=ALL_FORM_RECORDS`，逐日合计、九键合计和 `summary.formConsultationRecords` 必须一致。不得再用只覆盖可归因会话的旧聚合接口或 `submission_sessions` 反推表单总数。
- 百度统计固定 30 日基础快照使用 `baidu_tongji_snapshots`；网站流量任意日期范围使用 `baidu_tongji_range_snapshots`；所选来源趋势使用 `baidu_tongji_source_trend_snapshots`。已应用迁移不得改写，缓存表或索引调整只能新增迁移。
- 2026-08-04 官网全部表单记录的 `v3` 区间/逐日缓存、九键分类、独立迁移账本、首页 hook 和脱敏记录 adapter 已部署；独立只读凭据的本地对账中，2026-07-05 至 2026-08-04 共 21 条表单记录，九键与逐日合计一致。生产尚未注入 `GATO_WEBSITE_FORM_PROJECT_ID` 与专用只读账号凭据，因此正式页面诚实显示模块不可用；53KF 也未接入。不得把“代码已部署”描述为“官网或完整客服咨询已生产接通”。
